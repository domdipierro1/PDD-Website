# Final enquiry form fit fix

Applied a tighter internal layout so each enquiry form step fits inside the tile:

- Service options changed into compact small pills.
- Property size options changed into compact small pills.
- Address step compressed so postcode and address fields fit.
- Notes step compressed so consent text and buttons fit.
- Calendar now opens as a full-card overlay and hides the form controls until a date is selected.
- Calendar navigation is future-safe and no longer limited to near-term months.
