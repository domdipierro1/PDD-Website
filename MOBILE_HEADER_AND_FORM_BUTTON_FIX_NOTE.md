PDD Cleaning Services — Mobile header and enquiry button fix

Changes made:
- Removed the bottom fixed Call / Get Quote CTA from mobile by hiding .mobile-sticky-cta globally.
- The quote form Back / Next / Send buttons are fixed to the bottom on mobile instead, so customers do not need to scroll to find the next action.
- The mobile header is fixed, compact, and automatically hides once the user scrolls down. It appears again only when the page is back at the top or the menu is opened.
- The homepage hero, heading, progress area and form spacing were compressed on mobile so the enquiry form is visible much earlier without unnecessary scrolling.
- The previous CRM address payload fix remains unchanged.
