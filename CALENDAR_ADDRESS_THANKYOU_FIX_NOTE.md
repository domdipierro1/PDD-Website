# Calendar, address line 2 and thank-you fixes

Fixes applied:
- Calendar next-month arrow no longer conflicts with the quote form next-step button.
- Calendar can navigate forward, including at least 3 months ahead.
- Address line 2 optional label/pill removed.
- Thank-you page changed to a compact confirmation card so it fits on screen better.
- CSS/JS cache-busting updated so the browser loads the new files.
